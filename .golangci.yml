linters:
  enable-all: true
  fast: true
  disable:
  - gochecknoglobals
  - gochecknoinits
  - wsl
  - nlreturn
  - depguard
  - nosnakecase
  - ifshort
  - scopelint
  - copyloopvar

run:
  concurrency: 4
  timeout: 5m
  allow-parallel-runners: true
  allow-serial-runners: true